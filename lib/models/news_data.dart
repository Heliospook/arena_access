import 'package:flutter/material.dart';

class NewsData {
  String id;
  String author;
  String title;
  String content;
  DateTime timestamp;

  NewsData(this.id,this.author, this.title, this.content, this.timestamp);
}
